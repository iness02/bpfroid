#ifndef __ASM_LSE_H
#define __ASM_LSE_H
/* BPF stub: ARM64_LSE_ATOMIC_INSN already defined in bpf_compat.h
   Do NOT include atomic_lse.h — it uses physical register asm("x0")
   which is illegal in BPF's virtual ISA. */
#endif
